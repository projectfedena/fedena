D:/nithin/rails/fedena3/fedena2/app/controllers/calendar_controller.rb -- 99 warnings:
  CalendarController has at least 11 instance variables (LargeClass)
  CalendarController has no descriptive comment (IrresponsibleModule)
  CalendarController tests ("#{h.start_date.year}-#{h.start_date.month}-#{h.start_date.day}".to_date == "#{h.end_date.year}-#{h.end_date.month}-#{h.end_date.day}".to_date) at least 6 times (SimulatedPolymorphism)
  CalendarController tests ("#{h.start_date.year}-#{h.start_date.month}-#{h.start_date.day}".to_date == date) at least 6 times (SimulatedPolymorphism)
  CalendarController tests (@user.student == true) at least 3 times (SimulatedPolymorphism)
  CalendarController tests (d == date) at least 6 times (SimulatedPolymorphism)
  CalendarController tests employee_dept_event.nil? at least 4 times (SimulatedPolymorphism)
  CalendarController tests student_course_event.nil? at least 4 times (SimulatedPolymorphism)
  CalendarController#index calls 1.month twice (Duplication)
  CalendarController#index calls params 4 times (Duplication)
  CalendarController#index calls params[:new_month] 3 times (Duplication)
  CalendarController#index calls params[:new_month].to_i twice (Duplication)
  CalendarController#index has approx 9 statements (LongMethod)
  CalendarController#index has the variable name 'd' (UncommunicativeName)
  CalendarController#new_calendar calls 1.month twice (Duplication)
  CalendarController#new_calendar calls params 3 times (Duplication)
  CalendarController#new_calendar calls params[:new_month] twice (Duplication)
  CalendarController#new_calendar calls params[:new_month].to_i twice (Duplication)
  CalendarController#new_calendar has approx 9 statements (LongMethod)
  CalendarController#new_calendar has the variable name 'd' (UncommunicativeName)
  CalendarController#show_common_event_tooltip calls "#{h.end_date.year}-#{h.end_date.month}-#{h.end_date.day}".to_date twice (Duplication)
  CalendarController#show_common_event_tooltip calls "#{h.start_date.year}-#{h.start_date.month}-#{h.start_date.day}".to_date 3 times (Duplication)
  CalendarController#show_common_event_tooltip calls @common_event_array.push(h) twice (Duplication)
  CalendarController#show_common_event_tooltip calls h.end_date 6 times (Duplication)
  CalendarController#show_common_event_tooltip calls h.end_date.day twice (Duplication)
  CalendarController#show_common_event_tooltip calls h.end_date.month twice (Duplication)
  CalendarController#show_common_event_tooltip calls h.end_date.year twice (Duplication)
  CalendarController#show_common_event_tooltip calls h.start_date 9 times (Duplication)
  CalendarController#show_common_event_tooltip calls h.start_date.day 3 times (Duplication)
  CalendarController#show_common_event_tooltip calls h.start_date.month 3 times (Duplication)
  CalendarController#show_common_event_tooltip calls h.start_date.year 3 times (Duplication)
  CalendarController#show_common_event_tooltip contains iterators nested 2 deep (NestedIterators)
  CalendarController#show_common_event_tooltip has approx 8 statements (LongMethod)
  CalendarController#show_common_event_tooltip has the variable name 'd' (UncommunicativeName)
  CalendarController#show_common_event_tooltip has the variable name 'h' (UncommunicativeName)
  CalendarController#show_common_event_tooltip refers to h more than self (LowCohesion)
  CalendarController#show_exam_tooltip has approx 10 statements (LongMethod)
  CalendarController#show_exam_tooltip has the variable name 's' (UncommunicativeName)
  CalendarController#show_not_common_event_tooltip calls "#{h.end_date.year}-#{h.end_date.month}-#{h.end_date.day}".to_date 4 times (Duplication)
  CalendarController#show_not_common_event_tooltip calls "#{h.start_date.year}-#{h.start_date.month}-#{h.start_date.day}".to_date 6 times (Duplication)
  CalendarController#show_not_common_event_tooltip calls ("#{h.start_date.year}-#{h.start_date.month}-#{h.start_date.day}".to_date == "#{h.end_date.year}-#{h.end_date.month}-#{h.end_date.day}".to_date) twice (Duplication)
  CalendarController#show_not_common_event_tooltip calls ("#{h.start_date.year}-#{h.start_date.month}-#{h.start_date.day}".to_date == date) twice (Duplication)
  CalendarController#show_not_common_event_tooltip calls ("#{h.start_date.year}-#{h.start_date.month}-#{h.start_date.day}".to_date.."#{h.end_date.year}-#{h.end_date.month}-#{h.end_date.day}".to_date).each twice (Duplication)
  CalendarController#show_not_common_event_tooltip calls (d == date) twice (Duplication)
  CalendarController#show_not_common_event_tooltip calls @employee_dept_not_common_event_array.push(h) twice (Duplication)
  CalendarController#show_not_common_event_tooltip calls @student_course_not_common_event_array.push(h) twice (Duplication)
  CalendarController#show_not_common_event_tooltip calls @user.username twice (Duplication)
  CalendarController#show_not_common_event_tooltip calls CourseEvent.find_by_course_id(course.id, :conditions => ("event_id = #{h.id}")) twice (Duplication)
  CalendarController#show_not_common_event_tooltip calls EmployeeDepartmentEvent.find_by_employee_department_id(department.id, :conditions => ("event_id = #{h.id}")) twice (Duplication)
  CalendarController#show_not_common_event_tooltip calls course.id twice (Duplication)
  CalendarController#show_not_common_event_tooltip calls department.id twice (Duplication)
  CalendarController#show_not_common_event_tooltip calls employee_dept_event.nil? twice (Duplication)
  CalendarController#show_not_common_event_tooltip calls h.end_date 12 times (Duplication)
  CalendarController#show_not_common_event_tooltip calls h.end_date.day 4 times (Duplication)
  CalendarController#show_not_common_event_tooltip calls h.end_date.month 4 times (Duplication)
  CalendarController#show_not_common_event_tooltip calls h.end_date.year 4 times (Duplication)
  CalendarController#show_not_common_event_tooltip calls h.id 4 times (Duplication)
  CalendarController#show_not_common_event_tooltip calls h.start_date 18 times (Duplication)
  CalendarController#show_not_common_event_tooltip calls h.start_date.day 6 times (Duplication)
  CalendarController#show_not_common_event_tooltip calls h.start_date.month 6 times (Duplication)
  CalendarController#show_not_common_event_tooltip calls h.start_date.year 6 times (Duplication)
  CalendarController#show_not_common_event_tooltip calls non_common_events.each twice (Duplication)
  CalendarController#show_not_common_event_tooltip calls student_course_event.nil? twice (Duplication)
  CalendarController#show_not_common_event_tooltip contains iterators nested 2 deep (NestedIterators)
  CalendarController#show_not_common_event_tooltip has approx 19 statements (LongMethod)
  CalendarController#show_not_common_event_tooltip has the variable name 'd' (UncommunicativeName)
  CalendarController#show_not_common_event_tooltip has the variable name 'h' (UncommunicativeName)
  CalendarController#show_not_common_event_tooltip refers to h more than self (LowCohesion)
  CalendarController#view calls "#{h.end_date.year}-#{h.end_date.month}-#{h.end_date.day}".to_date 6 times (Duplication)
  CalendarController#view calls "#{h.start_date.year}-#{h.start_date.month}-#{h.start_date.day}".to_date 9 times (Duplication)
  CalendarController#view calls ("#{h.start_date.year}-#{h.start_date.month}-#{h.start_date.day}".to_date == "#{h.end_date.year}-#{h.end_date.month}-#{h.end_date.day}".to_date) 3 times (Duplication)
  CalendarController#view calls ("#{h.start_date.year}-#{h.start_date.month}-#{h.start_date.day}".to_date == date) 3 times (Duplication)
  CalendarController#view calls ("#{h.start_date.year}-#{h.start_date.month}-#{h.start_date.day}".to_date.."#{h.end_date.year}-#{h.end_date.month}-#{h.end_date.day}".to_date).each 3 times (Duplication)
  CalendarController#view calls (d == date) 3 times (Duplication)
  CalendarController#view calls @common_event_array.push(h) twice (Duplication)
  CalendarController#view calls @employee_dept_not_common_event_array.push(h) twice (Duplication)
  CalendarController#view calls @student_course_not_common_event_array.push(h) twice (Duplication)
  CalendarController#view calls @user.username twice (Duplication)
  CalendarController#view calls CourseEvent.find_by_course_id(course.id, :conditions => ("event_id = #{h.id}")) twice (Duplication)
  CalendarController#view calls EmployeeDepartmentEvent.find_by_employee_department_id(department.id, :conditions => ("event_id = #{h.id}")) twice (Duplication)
  CalendarController#view calls course.id twice (Duplication)
  CalendarController#view calls department.id twice (Duplication)
  CalendarController#view calls employee_dept_event.nil? twice (Duplication)
  CalendarController#view calls h.end_date 18 times (Duplication)
  CalendarController#view calls h.end_date.day 6 times (Duplication)
  CalendarController#view calls h.end_date.month 6 times (Duplication)
  CalendarController#view calls h.end_date.year 6 times (Duplication)
  CalendarController#view calls h.id 4 times (Duplication)
  CalendarController#view calls h.start_date 27 times (Duplication)
  CalendarController#view calls h.start_date.day 9 times (Duplication)
  CalendarController#view calls h.start_date.month 9 times (Duplication)
  CalendarController#view calls h.start_date.year 9 times (Duplication)
  CalendarController#view calls not_common_event.each twice (Duplication)
  CalendarController#view calls student_course_event.nil? twice (Duplication)
  CalendarController#view contains iterators nested 2 deep (NestedIterators)
  CalendarController#view has approx 26 statements (LongMethod)
  CalendarController#view has the variable name 'd' (UncommunicativeName)
  CalendarController#view has the variable name 'h' (UncommunicativeName)
  CalendarController#view refers to h more than self (LowCohesion)
