
<div class="label-field-pair">
  <label for="timetable_entry_course_id"><%= t('select_a_batch') %></label>
  <div class="text-input-bg"> <%= select :timetable_entry, :batch_id, @batches.map {|c| [c.full_name, c.id]},:prompt=>"#{t('select_a_batch')}" %>
  </div>
</div>
