<label class="head_label"><%= t('fees_submission_index.edit_fee_collection_date') %> <span></span></label>
<% form_remote_for :finance_fee_collection,
  :url => {:action => 'fee_collection_update', :id => @finance_fee_collection.id} do |form| %>
<div id="form-errors"></div>
  <div class="label-field-pair">
    <label for="reason"> <%= t('fee_collection_new.fee_category') %>:</label>
    <div class="text-input-bg"> <%= @finance_fee_collection.fee_category.name %></div>
  <div id ="batchs"></div>
  </div>
  <div class="label-field-pair">
    <label for="reason"> <%= t('fee_collection_new.course_for_category') %>:</label>
    <div class="text-input-bg"> <%= @finance_fee_collection.batch.full_name %></div>
  <div id ="batchs"></div>
  </div>
  <div class="label-field-pair">
    <label for="reason">  <%= t('fees_submission_index.fee_collection_name') %>:</label>
    <div class="text-input-bg"><%= form.text_field :name%></div>
  </div>
  <div class="label-field-pair">
    <label for="reason">  <%= t('layouts.default.start_date') %></label>
    <div class="text-input-bg"><%= calendar_date_select_tag 'fee_collection[start_date]',  @finance_fee_collection.start_date %></div>
  </div>
  <div class="label-field-pair">
    <label for="reason">  <%= t('layouts.default.end_date') %></label>
    <div class="text-input-bg"><%= calendar_date_select_tag 'fee_collection[end_date]',@finance_fee_collection.end_date %></div>
  </div>
  <div class="label-field-pair">
    <label for="reason">  <%= t('layouts.default.due_date') %></label>
    <div class="text-input-bg"><%= calendar_date_select_tag 'fee_collection[due_date]', @finance_fee_collection.due_date%></div>
  </div>

  <div class="extender"></div>
  <div id="submit-button">
        <%=  submit_tag "► #{t('layouts.default.update')}",:class=>'submit_button' %>
  </div>
<% end %>
