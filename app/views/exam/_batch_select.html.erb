<div class="label-field-pair">
  <label for="student_course"><%= t('select_a_batch') %> :</label>
  <div  class="text-input-bg">
    <%= select :ranking_level_report, :batch_id,
      @batches.map {|b| [b.full_name, b.id]},
      {:prompt => "#{t('select_a_batch')}"},{:onChange => "#{remote_function(:url => {:action => "select_report_type"},:with=>"'batch_id='+value")}"}%></div></div>
<div id="report_type_select"></div>
