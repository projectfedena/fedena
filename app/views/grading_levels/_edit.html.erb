
<div id="grading-levels-form">
  <h4><%= @batch.full_name unless @batch.nil? %></h4>

  <% form_remote_for @grading_level do |f| %>

<div id="form-errors"></div>

  <div class="label-field-pair">
    <div class="label-container"><%= f.label :"#{t('layouts.default.name')}" %></div>
    <div class="input-container"> <%= f.text_field :name %></div>
  </div>

    <div class="label-field-pair">
    <div class="label-container"><%= f.label :min_score, t('grading_levels.index.min_score') %></div>
    <div class="input-container"> <%= f.text_field :min_score %></div>
   </div>

    <%= submit_tag "", :value => "► #{t('layouts.default.save')}", :class => "submit_button" %>


  <% end %>
</div>
